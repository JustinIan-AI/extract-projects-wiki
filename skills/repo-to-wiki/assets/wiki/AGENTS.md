# AGENTS.md — {project_name} AI 协作操作手册

> **职责**：定义项目专属的开发规范，是 AI 进入项目后的"入职材料"
> **适用**：所有支持 AGENTS.md 的 AI 编程工具（Claude Code、Cursor、Copilot 等）
> **维护**：版本：v1.0 | 最后更新：{date}

---

## 1. 技术栈全景

### 后端
- **语言 runtime**：{backend_language}
- **框架**：{backend_framework}
- **ORM/数据访问**：{orm}
- **数据库**：{database}
- **缓存**：{cache}

### 前端（如有）
- **框架**：{frontend_framework}
- **状态管理**：{state_management}
- **UI 组件库**：{ui_library}
- **构建工具**：{build_tool}

### 基础设施
- **容器化**：{containerization}
- **CI/CD**：{cicd}
- **代码仓库结构**：{repo_structure}

---

## 2. 常用命令速查

```bash
# ============ 开发环境 ============
{dev_command}

# ============ 测试 ============
{test_command}

# ============ 构建 ============
{build_command}

# ============ 代码检查 ============
{lint_command}

# ============ 代码格式化 ============
{format_command}

# ============ 数据库 ============
{db_command}

# ============ 依赖管理 ============
{dependency_command}
```

---

## 3. 目录结构与模块边界

```
{project_name}/
├── src/                          # 源代码
│   ├── {controller_dir}          # [路由层：参数校验、路由转发]
│   ├── {service_dir}             # [业务逻辑层：核心业务处理]
│   ├── {repository_dir}          # [数据访问层：数据库操作封装]
│   ├── {model_dir}               # [数据模型：实体定义]
│   ├── {middleware_dir}          # [中间件：认证、日志、限流]
│   └── {utils_dir}               # [工具函数]
├── tests/                        # [测试文件，与 src 结构对应]
├── docs/                         # [文档目录]
├── scripts/                      # [运维脚本、数据库迁移等]
├── configs/                      # [配置文件]
└── [其他目录...]
```

### 禁止改动区域
> 以下目录/模块存在历史遗留问题，非必要不修改：
{prohibited_dirs}

---

## 4. 架构约束（CRITICAL）

### 4.1 分层规则（MUST 遵守）

| 层 | 职责 | 禁止行为 |
|----|------|----------|
| {controller_name} | 参数校验、路由转发 | ❌ 禁止写业务逻辑 |
| {service_name} | 业务逻辑处理 | ❌ 禁止直接操作 DB |
| {repository_name} | 数据访问封装 | ❌ 禁止包含业务判断 |

### 4.2 技术选型约束

| 场景 | 推荐方案 | 禁止方案 |
|------|----------|----------|
{tech_constraints_table}

---

## 5. 安全红线（CRITICAL）

> 🚨 以下规则必须严格遵守，违反将导致安全风险

### 5.1 数据安全
{data_security_rules}

### 5.2 输入校验
{input_validation_rules}

### 5.3 密钥管理
{secret_management_rules}

---

## 6. API 设计规范

### 6.1 响应格式（MUST 统一）

```
// ✅ 正确格式
{success_response_example}

// ❌ 禁止格式
{error_response_example}
```

### 6.2 路径命名
{path_naming_rules}

### 6.3 错误处理
{error_handling_rules}

---

## 7. Git 工作流

### 分支命名
```
feature/[JIRA-ID]-brief-description    # 功能分支
bugfix/[JIRA-ID]-brief-description      # Bug 修复
hotfix/[JIRA-ID]-brief-description      # 紧急修复
```

### Commit 格式（遵循 Conventional Commits）
```
feat:     新功能
fix:      Bug 修复
docs:     文档变更
style:    代码格式（不影响功能）
refactor: 重构（非新功能、非修复）
test:     测试相关
chore:    构建/工具变更
```

### PR 要求
- [MUST 包含测试用例]
- [MUST 通过 CI 检查（lint + test + build）]
- [MUST 至少 1 人 Review]
- [NEVER 直接向 main/master 分支推送]

---

## 8. 测试规范

### 测试框架
- {test_framework}

### 覆盖率要求
- {coverage_requirement}

### 测试文件位置
- {test_file_location}

### 必须覆盖的场景
- ✅ 正常业务流程
- ✅ 边界条件（空值、最大值、特殊字符）
- ✅ 异常场景（网络失败、权限不足、数据不存在）
- ❌ NEVER 在测试中连接真实数据库（使用 mock）

---

## 9. 经验教训（AI 犯过的错误记录）

> 以下是 AI 在本项目中曾经犯过的错误，防止重蹈覆辙

```markdown
## 历史教训
{lessons_learned}
```

---

## 10. 内部私有依赖

> 以下组件库不在 AI 训练数据中，使用前必须参考源码

```markdown
## 私有依赖
{private_dependencies}
```

---

## 11. 参考文档链接

- 架构设计文档：{architecture_doc_link}
- API 接口文档：{api_doc_link}
- 数据库 ER 图：{database_doc_link}
- 部署流程文档：{deployment_doc_link}

---

## 📌 渐进式披露原则

> 以下是详细规范文档，通过链接引用，避免主文件过于臃肿

- 详细架构规范 → [./docs/architecture.md]
- 详细测试规范 → [./docs/testing-guide.md]
- 安全合规说明 → [./docs/security.md]
- 数据库设计规范 → [./docs/database.md]

---

*AGENTS.md 主文件应控制在 100-150 行以内，详细信息通过链接引用。*

**Generated by**: repo-to-wiki skill
**Generated on**: {date}